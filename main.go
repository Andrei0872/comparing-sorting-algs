package main

import (
	"fmt"
)

/*
* 5 sorting + the native one
* for each run a test SUITE
  * each test suite must have
    * a number of tests
    * nr of elements
    * nr maxim
*/

func main() {
	fmt.Println("hellO!")
}
